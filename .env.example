# Netboot Server - Environment Configuration
# Copy this file to .env and adjust values for your network

# IP address of the Docker host on the LAN
# This is the IP that PXE clients will connect to for TFTP and HTTP
TFTP_SERVER_IP=192.168.1.100

# HTTP port for serving boot files and OS images
HTTP_PORT=8080

# Network interface to listen on (for dnsmasq)
DHCP_INTERFACE=eth0

# Boot menu title displayed to clients
MENU_TITLE="Netboot Server"

# Subnet allowed to access the SMB share (for Windows PXE installs)
# Default: derived from TFTP_SERVER_IP (last octet replaced with 0/24)
SMB_ALLOWED_SUBNET=192.168.1.0/24
